FROM ubuntu:18.04 # Указываем образ, от которого берётся наследование

RUN apt update -y # Обновляем индекс пакетов
RUN apt install -y python3 # Устанавливаем интерпретатор

CMD ["python3"] # Указываем команду, которая должна выполниться при старте контейнера